#summary Asssignment 1
<a href="https://code.google.com/p/system-programming-cs3402-at-crma/wiki/CourseSchedule2555_2#ตารางเรียน_๒๕๕๕/๒">ตารางเรียน</a>

==จุดประสงค์==
<font color=grey>
  * เพื่อให้นักเรียนรู้จักใช้งาน ssh client เพื่อล็อกอินเข้าไปยังเครื่องแม่ข่าย
  * เพื่อให้นักเรียนสามารถใช้คำสั่งต่างของ shell ผ่าน terminal console ได้
  * ให้นักเรียนรู้จักคำสั่ง history และการทำ redirection ของเอาท์พุท
  * เพื่อเก็บข้อมูล email ของนักเรียนแล้วนำมาสร้างเป็น group สำหรับการสื่อสารระหว่างอาจารย์กับนักเรียน
</font>

==งานมอบ==
  # ติดตั้งโปรแกรม ssh client บนเครื่องของตนเอง หากยังไม่มี
  # ล็อกอินไปยังเครื่องแม่ข่าย 10.134.64.45 ด้วยชื่อผู้ใช้ และรหัสผ่าน ที่กำหนดให้
  # รันคำสั่งต่อไปนี้ (ทีละคำสั่งตามด้วย return) แล้วศึกษา/สังเกตุการทำงานของแต่ละคำสั่ง ls, whoami, ifconfig, ps, ls  /, free,  top (ออกจากโปรแกรมด้วย q), ping 10.134.64.1 (ออกจากโปรแกรมด้วย ctrl-c)
  # สร้างไดเร็กทอรี่ asm01 ด้วยคำสั่ง
{{{
mkdir   asm01
}}}
  # สุดท้ายรันคำสั่งต่อไปนี้  history  > asm01-cXX  (ให้แทน XX ด้วยเลขที่ของตนเอง เช่น เลขที่ ๕  จะต้องรันคำสั่งด้วย history > asm01-c05 เป็นต้น) 
  # เพื่อดำรงการสื่อสารระหว่างอาจารย์-นักเรียน ให้มีประสิทธิภาพ นนร. ทุกนาย ส่งอีเมลถึง pyavinci@gmail.com และให้ CC: phnarong@hotmail.com โดยให้ใส่หัวเรื่อง ว่า CS3402_ ตามด้วย เลขที่ โดยในตัวข้อความให้ใส่ข้อมูล  ชื่อ นนร. ------- เลขที่ ------ 

----
<font color=red>Due date: 2012-09-18</font>
----

สามารถดูผลการส่งงานได้ที่ [AssignmentSummary]