#summary Asssignment 1
[CourseSchedule2555_2 กลับไปหน้าหลัก]

  # ติดตั้งโปรแกรม ssh client บนเครื่องของตนเอง หากยังไม่มี
  # ล็อกอินไปยังเครื่องแม่ข่าย 10.134.64.45 ด้วยชื่อผู้ใช้ และรหัสผ่าน ที่กำหนดให้
  # รันคำสั่งต่อไปนี้ (ทีละคำสั่งตามด้วย return) แล้วศึกษา/สังเกตุการทำงานของแต่ละคำสั่ง ls, whoami, ifconfig, ps, ls  /, free,  top (ออกจากโปรแกรมด้วย q), ping 10.134.64.1 (ออกจากโปรแกรมด้วย ctrl-c)
  # สร้างไดเร็กทอรี่ asm01 ด้วยคำสั่ง
{{{
mkdir   asm01
}}}
  # สุดท้ายรันคำสั่งต่อไปนี้  history  > asm01-cXX  (ให้แทน XX ด้วยเลขที่ของตนเอง เช่น เลขที่ ๕  จะต้องรันคำสั่งด้วย history > asm01-c05 เป็นต้น) 